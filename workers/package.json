{
  "name": "eternlink-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for EternLink",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240125.0",
    "typescript": "^5.2.2",
    "wrangler": "^3.28.0"
  }
}

